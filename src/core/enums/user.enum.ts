export enum UserStatuses {
  NEW = 'new',
  WAIT = 'wait',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  BLOCKED = 'blocked',
  DELETED = 'deleted',
}

export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}
